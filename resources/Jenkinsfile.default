node {
    stage('Build') {
        echo 'Building with default pipeline'
    }

    stage('Test') {
        echo 'Running tests for default pipeline'
    }
    stage('Print Message') {
        echo "Hey ${user}, you smell ${params.SMELL}"
    }
}
