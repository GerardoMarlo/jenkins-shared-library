
node {
    stage('Setup for Vendor') {
            echo 'Setting up environment for Vendor...'
    }
    stage('Build') {
            echo 'Building the project for Vendor...'
        
    }
    stage('Deploy') {
            echo 'Deploying Vendor application...'
    }
    stage('Print Message') {
        echo "Hey ${env.USER}, you smell ${params.SMELL}"
    }
}

